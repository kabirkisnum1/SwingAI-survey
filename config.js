// API endpoint for submitting survey responses.
// When deployed, set this to your backend URL if the survey is hosted separately.
// Example: "https://swing-survey-api.onrender.com/api/responses"
window.SURVEY_CONFIG = {
  apiUrl: "/api/responses",
};
